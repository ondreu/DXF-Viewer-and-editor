// Type surface for the build-time generated worker bundle string.
// The concrete `inline.generated.ts` is produced by esbuild.config.mjs and is
// git-ignored; tsc reads this declaration instead (the .ts is excluded).
export const WORKER_SOURCE: string;
